@startuml
start

:1. Enter Username, Email, Password, Confirm Password;

if (2. Is Username or Email duplicate?) then (Yes)
  :6. Show error "Username or Email already exists";
  -1. [Restart] back to start
else (No)
  if (3.Do Password and Confirm Password match?) then (No)
    :7. Show error "Passwords do not match";
    -1. [Restart] back to start
  else (Yes)
    if (4.Is Email format valid?) then (No)
      :8. Show error "Invalid Email Format";
     -1. [Restart] back to start
    else (Yes)
      :4. Create Account;
      :5. Show success message;
      stop
    endif
  endif
endif

@enduml
