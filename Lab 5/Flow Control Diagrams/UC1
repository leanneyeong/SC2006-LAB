@startuml
title UC001 - Control Flow Diagram (User Account Creation)
start

:1. Enter First name, Last name, Email address, Password;

if (2. Is First name or Last name filled?) then (Yes)
    if (3. Is Email format valid?) then (Yes)
      :5. Show error "Please match the requested format.";
      -1. Allow user to try filling out the field again
      stop
    else (No)
        if (6. Is Email address duplicate?) then (No)
            :7. Show error "This email address is taken. Please try another.";
            -1. Allow user to try filling out the field again
            stop
        else (Yes)
            if (8. Does password meet all the requirements?) then (Yes)
                :9. Create Account;
                stop
            else (No)
                :10. Show relevant error based on requirement that has not been met;
                -1. Allow user to try filling out the field again
                stop
            endif
        endif
    endif
else (No)
    :4. Show error "Please fill out this field";
    -1. Allow user to try filling out the field again
    stop
endif

@enduml