@startuml
start
title UC004 - Control Flow Diagram (Password Change)

:1. User enters New Password and Confirm New Password;

if (2. Does New Password meet the requirements?) then (Yes)
    if (4. Do the password fields match?) then (Yes)
        :6. Update password;
        stop
    else (No)
        :5. Show error “New passwords do not match”;
        -1. Allow user to try filling out the field again
        stop
    endif
else (No)
    :3. Show error "Failed to update password”  when User attempts to update password;
    -1. Allow user to try filling out the field again
    stop
endif
@enduml
