@startuml
start
title UC003 - Control Flow Diagram (Name Change)

:1. User enters First name and Last name;

if (2. Is first name empty?) then (Yes)
  :3. User is unable to click "Continue";
  stop
else (No)
  if (4. Is last name empty?) then (Yes)
    :5. User is unable to click "Continue";
    stop
  else (No)
    :6. Update name in system;
    :7. Display success message "Your profile has been updated";
    stop
  endif
endif

@enduml
