@startuml
title UC006 - Control Flow Diagram (Admin Password Recovery)
start

:1. Admin enters email for password recovery;
:2. Submit recovery request;

if (3. Is email valid?) then (yes)
  :4. Proceed to send password recovery link;
else (no)
  :5. Display invalid email error;
  - 1. Re-enter email
  stop
endif

if (6. Is email registered?) then (yes)
  :7. Send password recovery link;
  :8. Display recovery link sent message;
  :9. Redirect to password reset page;
  stop
else (no)
  :10. Display email not found error;
  - 1. Re-enter email
  stop
endif

@enduml
