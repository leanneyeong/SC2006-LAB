@startuml
start

:1. Admin logs in with special access;
:2. Select "Create Admin Account";
:3. Enter Username, Email, Password, Confirm Password;

if (4. Is Username or Email duplicate?) then (Yes)
  :9. Show error "Username or Email already exists";
  -3. [Restart] back to start
else (No)
  if (5.Do Password and Confirm Password match?) then (No)
    :10. Show error "Passwords do not match";
    -3. [Restart] back to start
  else (Yes)
    if (6. Is Email format valid?) then (No)
      :11. Show error "Invalid Email Format";
     -3. [Restart] back to start
    else (Yes)
      :7. Create Account;
      :8. Show success message;
      stop
    endif
  endif
endif

@enduml
